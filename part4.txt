    // create a network
    var container = document.getElementById('mynetwork');
    var data = {
        nodes: nodes,
        edges: edges
    };
    var options = {};
    var network = new vis.Network(container, data, options);
//    network.setSize(String(window.innerWidth)+'px', String(window.innerHeight));
</script>

<p id="credit">
    <br>
    This webpage uses <a href='https://github.com/almende/vis'><strong>vis.js</strong></a> library.
    Thanks to Almende B.V. and all Other Contributors for developing this awesome project!
</p>

</body>
</html>
